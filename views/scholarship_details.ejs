<body>
    <main>
        <!-- Hero Area Start-->
        <div class="slider-area ">
            <div class="single-slider section-overly slider-height2 d-flex align-items-center"
                data-background="assets/img/hero/dg.png">
                <div class="container">
                    <div class="row">
                        <div class="col-xl-12">
                            <div class="hero-cap text-center">
                                <h2>Scholarship Details</h2>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <!-- Hero Area End -->

        <!-- Job Details Start -->
        <div class="job-post-company pt-120 pb-120">
            <div class="container">
                <div class="row justify-content-between">
                    <!-- Left Content -->
                    <div class="col-xl-7 col-lg-8">
                        <!-- job single -->
                        <div class="single-job-items mb-50">
                            <div class="job-items">
                                <div class="company-img company-img-details">
                                    <a href="#"><img src="/assets/img/icon/job-list1.png" alt=""></a>
                                </div>
                                <div class="job-tittle">
                                    <a href="#">
                                        <h4>
                                            <%= scholarship ? scholarship.programName : 'Program Name' %>
                                        </h4>
                                    </a>
                                    <ul>
                                        <li>
                                            <%= scholarship ? scholarship.collegeName : 'College Name' %>
                                        </li>
                                        <li><i class="fas fa-map-marker-alt"></i>
                                            <%= scholarship ? scholarship.sponsorship : 'Sponsorship' %>
                                        </li>
                                        <li>
                                            <%= scholarship ? scholarship.vacancy : 'Vacancy' %>
                                        </li>
                                    </ul>
                                </div>
                            </div>
                        </div>
                        <!-- job single End -->

                        <div class="job-post-details">
                            <div class="post-details1 mb-50">
                                <!-- Small Section Tittle -->
                                <div class="small-section-tittle">
                                    <h4>Program Description</h4>
                                </div>
                                <p>
                                    <%= scholarship ? scholarship.description : 'No description available' %>
                                </p>
                            </div>

                            <div class="post-details2  mb-50">
                                <!-- Small Section Tittle -->
                                <div class="small-section-tittle">
                                    <h4>Requirements</h4>
                                </div>
                                <ul>
                                    <li>
                                        <%= scholarship ? scholarship.requirements
                                            : 'Requirements information not available' %>
                                    </li>
                                </ul>
                            </div>
                        </div>

                    </div>
                    <!-- Right Content -->
                    <div class="col-xl-4 col-lg-4">
                        <div class="post-details3  mb-50">
                            <!-- Small Section Tittle -->
                            <div class="small-section-tittle">
                                <h4>Scholarship Overview</h4>
                            </div>
                            <ul>
                                <li>Posted date : <span>
                                        <%= scholarship ? new Date(scholarship.createdAt).toLocaleDateString('en-US', {
                                            year: 'numeric' , month: 'short' , day: 'numeric' }) : 'N/A' %>
                                    </span></li>
                                <li>College : <span>
                                        <%= scholarship ? scholarship.collegeName : 'N/A' %>
                                    </span></li>
                                <li>Sponsorship : <span>
                                        <%= scholarship ? scholarship.sponsorship : 'N/A' %>
                                    </span></li>
                                <li>Vacancy : <span>
                                        <%= scholarship ? scholarship.vacancy : 'N/A' %>
                                    </span></li>
                                <li>Deadline : <span>
                                        <%= scholarship ? new Date(scholarship.deadline).toLocaleDateString('en-US', {
                                            year: 'numeric' , month: 'short' , day: 'numeric' }) : 'N/A' %>
                                    </span></li>
                                <li>Status : <span>
                                        <%= scholarship && scholarship.status==='Active' ? 'Open' : scholarship ?
                                            scholarship.status : 'N/A' %>
                                    </span></li>
                            </ul>
                            <div class="apply-btn2">
                                <% if (scholarship && scholarship.status==='Closed' ) { %>
                                    <a href="#" class="btn"
                                        style="opacity: 0.6; cursor: not-allowed; pointer-events: none;">Apply Now
                                        (Closed)</a>
                                    <% } else { %>
                                        <a href="#" class="btn" data-toggle="modal"
                                            data-target="#applicationModal">Apply Now</a>
                                        <% } %>
                            </div>
                        </div>
                        <div class="post-details4  mb-50">
                            <!-- Small Section Tittle -->
                            <div class="small-section-tittle">
                                <h4>Organization Information</h4>
                            </div>
                            <span>
                                <%= scholarship ? scholarship.collegeName : 'Organization' %>
                            </span>
                            <p>It is a long established fact that a reader will be distracted by the readable content of
                                a page when looking at its layout.</p>
                            <ul>
                                <li>Name: <span>
                                        <%= scholarship ? scholarship.collegeName : 'N/A' %>
                                    </span></li>
                                <li>Program : <span>
                                        <%= scholarship ? scholarship.programName : 'N/A' %>
                                    </span></li>
                                <li>Sponsorship: <span>
                                        <%= scholarship ? scholarship.sponsorship : 'N/A' %>
                                    </span></li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <!-- Job Details End -->

        <!-- Related Scholarships Start -->
        <section class="featured-job-area feature-padding">
            <div class="container">
                <!-- Section Tittle -->
                <div class="row">
                    <div class="col-lg-12">
                        <div class="section-tittle text-center">
                            <span>Recent Scholarships</span>
                            <h2>Related Scholarships</h2>
                        </div>
                    </div>
                </div>
                <div class="row justify-content-center">
                    <div class="col-xl-10">
                        <!-- Loop through related scholarships -->
                        <div class="single-job-items mb-30">
                            <p class="text-center">More scholarship opportunities coming soon!</p>
                        </div>
                    </div>
                </div>
            </div>
        </section>
        <!-- Related Scholarships End -->

        <!-- Application Modal Start -->
        <div class="modal fade" id="applicationModal" tabindex="-1" role="dialog"
            aria-labelledby="applicationModalLabel" aria-hidden="true">
            <div class="modal-dialog modal-xl" role="document" style="max-width: 90%; width: 90%;">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="applicationModalLabel">Apply for Scholarship</h5>
                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                    <div class="modal-body" style="max-height: 70vh; overflow-y: auto; padding: 30px;">
                        <form class="form-contact contact_form" action="/scholarship-application" method="post"
                            id="applicationForm" novalidate="novalidate" enctype="multipart/form-data">

                            <!-- Hidden Scholarship ID -->
                            <input type="hidden" name="scholarshipId" id="scholarshipId" value="<%= scholarship ? scholarship.id : '' %>">

                            <div class="row">

                                <!-- First Name -->
                                <div class="col-sm-6">
                                    <div class="form-group">
                                        <input class="form-control" name="firstName" id="firstName" type="text"
                                            placeholder="First Name" required>
                                    </div>
                                </div>

                                <!-- Last Name -->
                                <div class="col-sm-6">
                                    <div class="form-group">
                                        <input class="form-control" name="lastName" id="lastName" type="text"
                                            placeholder="Last Name" required>
                                    </div>
                                </div>

                                <!-- Email -->
                                <div class="col-sm-6">
                                    <div class="form-group">
                                        <input class="form-control" name="email" id="email" type="email"
                                            placeholder="Email" required>
                                    </div>
                                </div>

                                <!-- Phone Number -->
                                <div class="col-sm-6">
                                    <div class="form-group">
                                        <input class="form-control" name="phone" id="phone" type="text"
                                            placeholder="Phone Number" required>
                                    </div>
                                </div>

                                <!-- Physical Address -->
                                <div class="col-12">
                                    <div class="form-group">
                                        <input class="form-control" name="address" id="address" type="text"
                                            placeholder="Your Physical Address" required>
                                    </div>
                                </div>

                                <!-- Passport Upload -->
                                <div class="col-12">
                                    <div class="form-group">
                                        <label for="passport" class="form-label">Upload Passport (PDF only)</label>
                                        <input class="form-control" name="passport" id="passport" type="file"
                                            accept=".pdf" required>
                                        <small class="text-muted">Upload a clear PDF copy (Max 5MB)</small>
                                    </div>
                                </div>

                            </div>

                            <div class="form-group mt-3">
                                <button type="submit" class="button button-contactForm boxed-btn">Submit
                                    Application</button>
                                <button type="button" class="btn btn-secondary ml-2"
                                    data-dismiss="modal">Cancel</button>
                            </div>

                        </form>


                        <!-- Success/Error Messages -->
                        <div class="mt-4">
                            <div id="successMessage" class="alert alert-success" style="display: none;">
                                <strong>Success!</strong> Your application has been submitted successfully. We'll get
                                back to you soon.
                            </div>
                            <div id="errorMessage" class="alert alert-danger" style="display: none;">
                                <strong>Error!</strong> There was a problem submitting your application. Please try
                                again.
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <!-- Application Modal End -->

        <!-- JavaScript for Modal and Form Handling -->
        <script>
            $(document).ready(function () {
                // Show success/error messages based on URL parameters
                const urlParams = new URLSearchParams(window.location.search);
                if (urlParams.get('success') === 'true') {
                    $('#successMessage').show();
                }
                if (urlParams.get('error') === 'true') {
                    $('#errorMessage').show();
                }

                // Form validation and submission
                $('#applicationForm').on('submit', function (e) {
                    e.preventDefault();
                    
                    const requiredFields = ['firstName', 'lastName', 'email', 'phone', 'address', 'passport'];
                    let isValid = true;

                    requiredFields.forEach(field => {
                        const input = $('#' + field);
                        if (field === 'passport') {
                            if (!input.val()) {
                                input.css('border-color', 'red');
                                isValid = false;
                            } else {
                                input.css('border-color', '');
                            }
                        } else {
                            if (!input.val().trim()) {
                                input.css('border-color', 'red');
                                isValid = false;
                            } else {
                                input.css('border-color', '');
                            }
                        }
                    });

                    if (!isValid) {
                        alert('Please fill in all required fields');
                        return;
                    }

                    // Submit form via AJAX
                    const formData = new FormData(this);
                    
                    $.ajax({
                        url: '/scholarship-application',
                        type: 'POST',
                        data: formData,
                        processData: false,
                        contentType: false,
                        success: function (response) {
                            if (response.success) {
                                $('#successMessage').show();
                                $('#applicationForm')[0].reset();
                                setTimeout(() => {
                                    $('#applicationModal').modal('hide');
                                    window.location.reload();
                                }, 1500);
                            }
                        },
                        error: function (xhr) {
                            const response = xhr.responseJSON;
                            $('#errorMessage').text('Error: ' + (response.message || 'Error submitting application')).show();
                        }
                    });
                });

                // Reset form when modal is closed
                $('#applicationModal').on('hidden.bs.modal', function () {
                    $('#applicationForm')[0].reset();
                    // Reset any error styling
                    $('#applicationForm input, #applicationForm textarea').css('border-color', '');
                    $('#successMessage, #errorMessage').hide();
                });
            });
        </script>

    </main>
</body>